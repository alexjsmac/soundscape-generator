{
  "name": "soundscape-generator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "gunicorn server.app:app",
    "build": "npm run build --workspaces --if-present"
  },
  "workspaces": [
    "client"
  ]
}